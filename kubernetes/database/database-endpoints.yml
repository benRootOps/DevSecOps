apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: mariadb-external-1
  namespace: univora
  labels:
    kubernetes.io/service-name: mariadb-external
addressType: IPv4
ports:
  - port: 3306
endpoints:
  - addresses:
      - 10.0.2.15
