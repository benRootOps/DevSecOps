apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: univora
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      targetPort: 5173
      port: 5173
  type: ClusterIP
